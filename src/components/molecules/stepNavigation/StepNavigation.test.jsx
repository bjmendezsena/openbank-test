import React from "react";
import { shallow } from "enzyme";
import { StepNavigation } from "./StepNavigation";

describe("Test inn StStepNavigationepper component", () => {
  it("Should display the component", () => {
    shallow(<StepNavigation />);
  });
});
